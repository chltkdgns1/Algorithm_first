#include <iostream>
#include <vector>
using namespace std;

int dp[10001];
vector <pair<int, int>> v;

int solve( )
int main() {
	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int a, b; cin >> a >> b;
	for (int i = 0; i < b; i++) {
		int c, d; cin >> c >> d;
		v.push_back({ c,d });
	}
}