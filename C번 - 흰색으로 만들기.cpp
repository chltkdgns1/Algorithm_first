#include <iostream>
using namespace std;

char map[2001][2001];
int a, b;

int main() {
	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	cin >> a >> b;
	for (int i = 0; i < a; i++) cin >> map[i];
}